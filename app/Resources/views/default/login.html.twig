{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <h2 id="h2_message_de_bienvenue">Bonjour et Bienvenue sur notre site de Location de BD</h2>
        <p>
            Sur ce site vous aurez la possibilité de louer vos BDs 
            à prix réduit* grâce à la mairie de Paris.
        </p>

        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData) }}</div>
        {% endif %}
        <p>Pour commencer veuillez vous identifier : </p>
        <form class="form-signin" action="{{ path('login_check') }}" method="post">

            <label for="inputEmail" class="sr-only">Email address</label>
            <input type="email" id="inputEmail" class="form-control" name="_username" placeholder="Email address" required autofocus>
            <label for="inputPassword" class="sr-only">Password</label>
            <input type="password" id="inputPassword" class="form-control" name="_password" placeholder="Password" required>
            <button class="btn btn-lg btn-primary btn-block" type="submit">Connexion</button>
        </form>
        <p id="register">
            Si vous n'êtes pas déjà membre, vous pouvez cliquer sur ce lien pour vous enregistrer :
            <a href="{{ path('register_user') }}" title="Bdloc | Register "> S'enregistrer</a>
        </p>

        <p id="ptite_ligne">
            *Offre réservée aux citoyens résidents de la ville de Paris. 
            Pour les résidents de tout autre ville un abonnement d'un montant de 1250€ par mois sera demandé. 
            
        </p>
    </div>
{% endblock %}
